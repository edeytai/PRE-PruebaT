import { Routes } from '@angular/router';

/**
 * Rutas de la aplicación.
 *
 * Las rutas concretas (listado y detalle) se agregan en commits posteriores
 * a medida que los componentes correspondientes existen. Esta estructura
 * inicial deja el shell de routing listo para crecer.
 */
export const APP_ROUTES: Routes = [];
